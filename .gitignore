# .gitignore generated to exclude large files preventing pushes
# Model checkpoints (PyTorch, TensorFlow, etc.)
*.pth
*.pt
*.pkl
# Directory containing generated checkpoints
experiments/checkpoints/
# Large processed image data
data/processed/
data/processed_fruit/
# Optionally raw data (if not needed in repo)
data/raw/
